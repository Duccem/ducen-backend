import { FacebookStrategy, GithubStrategy, GoogleStrategy, JWTStrategy, LinkedinStrategy, TwitterStrategy } from '@ducen/adaptors';

export const strategies = [JWTStrategy, FacebookStrategy, GoogleStrategy, TwitterStrategy, LinkedinStrategy, GithubStrategy];
